# assignment-service

Авто-назначение агента (round-robin L1). Слушает `helpdesk.ticket.created`, вызывает `AssignTicket` в ticket-service.

Часть monorepo [HelpDesk](https://github.com/Glistand/HelpDesk).
